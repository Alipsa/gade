package se.alipsa.gade.environment.connections;

/**
 * Enum representing the type of connection.
 */
public enum ConnectionType {
  /**
   * Relational database connection type.
   */
  JDBC,
  /**
   * Google BigQuery connection type.
   */
  BIGQUERY
}
